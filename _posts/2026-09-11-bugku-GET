---
title: BugkuCTF-GET Writeup
date: 2026-09-11
categories: CTF
tags: [Web, PHP, GET参数]
---

# Bugku CTF - GET Writeup

## 题目描述
打开页面，直接显示一段PHP源码：
```php
$what=$_GET['what'];
echo $what;
if($what=='flag')
echo 'flag{****}';

##解题思路

这道题考察PHP的GET传参。$_GET['what']用于接收URL中GET方式传递的what参数。
当传入的参数what的值等于flag时，代码就会输出flag。

##解题步骤

1. 在原网页URL的末尾，添加GET参数：?what=flag

2. 拼接后的完整URL格式：原地址?what=flag

3. 访问拼接后的URL，页面返回flag。

##Flag

flag{c864c478ecf15f5a44a2ee54cdc37a6e}

##总结

GET参数是Web基础知识点，GET参数附加在URL后方，服务端读取参数进行判断，构造对应参数拿到flag。
