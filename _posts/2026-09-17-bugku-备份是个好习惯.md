---
Title: Bugku CTF - A Good Habit to Make Backups
Category: Web
Date: 2026-09-18
Tags: [Backup file leakage, str_replace double-write bypass, PHP MD5 loose comparison, Bugku]
---
## 题目描述
题目提示“备份是个好习惯”，需要找到源码并绕过PHP的校验逻辑获取flag。

## 获取源代码
根据提示访问备份文件 `index.php.bak`，下载得到PHP源码：
```php
<?php
include_once "flag.php";
ini_set("display_errors", 0);
$str = strstr($_SERVER['REQUEST_URI'], '?');
$str = substr($str,1);
$str = str_replace('key','',$str);
parse_str($str);
echo md5($key1);
echo md5($key2);
if(md5($key1) == md5($key2) && $key1 !== $key2){
    echo $flag."取得flag";
}
?>

